#include <stdlib.h>
#include <stdio.h>
#include <math.h>

int main(){
    double h = sqrt((9 - 7)*(9 - 7) + ((10-4)* (10-4)));
    int tmp = h -(int)h;
    // cout<<(int)7.2;
    fprintf(stdout, "%d", (int)7.2);
}